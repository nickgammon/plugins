<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE muclient>

<muclient>
<plugin
   name="Room_Contents_Telnet"
   author="Nick Gammon"
   id="a02de78ffbef7b149c6b0805"
   language="Lua"
   purpose="Shows contents of room in a miniwindow"
   date_written="2010-02-02"
   requires="4.40"
   version="1.0"
   save_state="y"
   >
<description trim="y">
<![CDATA[
Install this plugin to show what is in the current room.

The window can be dragged to a new location with the mouse.
]]>
</description>

</plugin>


<!--  Script  -->


<script>
<![CDATA[

FONT_NAME = "Dina"    -- the font we want to use
FONT_SIZE = 8
FONT_ID = "fn"  -- internal font identifier
  
require "movewindow"

function OnPluginInstall ()
  
  win = GetPluginID ()

  WindowCreate (win, 0, 0, 0, 0, 0, 0, 0)
                 
  -- add the font
  WindowFont (win, FONT_ID, FONT_NAME, FONT_SIZE)
  
  -- see how high it is
  font_height = WindowFontInfo (win, FONT_ID, 1)  -- height

  -- find where window was last time
  windowinfo = movewindow.install (win, 7)
   
end -- OnPluginInstall

function OnPluginEnable ()
  WindowShow (win, true)
end -- OnPluginDisable

function OnPluginDisable ()
  WindowShow (win, false)
end -- OnPluginDisable

-- hide window on removal
function OnPluginClose ()
  WindowShow (win,  false)  -- hide it
end -- OnPluginClose

function OnPluginSaveState ()
  movewindow.save_state (win)
end -- OnPluginSaveState

-- here when location changes
--[[
 example line (extra lines added for clarity): 
 
 tt="move";room=21056;name="Quills and Parchments";
 blind=false;dark=false;
 exits={north=21047;west=21053;};
 
--]]

item_colours = {
  none = "black",
  light = "yellow",
  scroll = "cyan",
  wand = "cyan",
  staff = "cyan",
  weapon = "black",
  _fireweapon = "black",
  _missile = "black",
  treasure = "black",
  armor = "black",
  potion = "black",
  _worn = "black",
  furniture = "black",
  trash = "black",
  _oldtrap = "black",
  container = "black",
  _note = "black",
  drinkcon = "blue",
  key = "black",
  food = "sienna",
  money = "gold",
  pen = "black",
  boat = "black",
  corpse = "gray",
  corpse_pc = "gray",
  fountain = "blue",
  pill = "cyan",
  blood = "red",
  bloodstain = "red",
  scraps = "black",
  pipe = "black",
  herbcon = "black",
  herb = "green",
  incense = "black",
  fire = "red",
  book = "black",
  switch = "black",
  lever = "black",
  pullchain = "black",
  button = "black",
  dial = "black",
  rune = "cyan",
  runepouch = "cyan",
  match = "black",
  trap = "black",
  map = "black",
  portal = "black",
  paper = "black",
  tinder = "black",
  lockpick = "black",
  spike = "black",
  disease = "black",
  oil = "black",
  fuel = "black",
  _empty1 = "black",
  _empty2 = "black",
  missileweapon = "black",
  projectile = "black",
  quiver = "black",
  shovel = "black",
  salve = "black",
  cook = "black",
  keyring = "black",
  odor = "black",
  chance = "black",
  mix = "black",
   }
   
function OnPluginTelnetOption (option)

  local t = {}  -- incoming server variables will go into table t
  setfenv (assert (loadstring (option)), t) () -- compile and load into t
  
  if t.tt ~= "inroom" then
    return
  end
  
  local width = 0
  local lines = 1  -- have at least one line
  local dark_message = "It is too dark to see."
  local blind_message = "You are blind!"
  local nothing_message = "There is nothing here."
  local count = 0
  
  local background_colour = ColourNameToRGB "darkkhaki"
  
  if t.dark then
    width = WindowTextWidth (win, FONT_ID, dark_message)
  elseif t.blind then
    width = WindowTextWidth (win, FONT_ID, blind_message)
  else  
    if t.inroom then
      for k, v in pairs (t.inroom) do
        for i, item in ipairs (v) do
          if item.lvl then
            item.dsc = string.format ("%s [lvl %i]", item.dsc, item.lvl)
          end -- if
          width = math.max (width, 10 + WindowTextWidth (win, FONT_ID, item.dsc))
          lines = lines + 1
          count = count + 1
        end -- for
      end -- for each item
    end -- if t.inroom table 
    if count == 0 then
      width = WindowTextWidth (win, FONT_ID, nothing_message)
    else 
      lines = lines - 1
    end -- if  
  end -- if
  
  window_width = width + 10
  window_height = font_height * lines + 10
  
  WindowCreate (win, 
                 windowinfo.window_left, 
                 windowinfo.window_top, 
                 window_width, window_height,  
                 windowinfo.window_mode,   -- top right
                 windowinfo.window_flags,
                 0) 

  -- let them move it around                 
  movewindow.add_drag_handler (win, 0, 0, 0, 0)
    
  
  -- fill entire box to clear it
  WindowRectOp (win, 2, 0, 0, 0, 0, background_colour)  -- fill entire box
   
  -- Edge around box rectangle
  WindowCircleOp (win, 3, 0, 0, 0, 0, ColourNameToRGB "darkgray", 0, 2, 0, 1)

  if t.blind then
    WindowText (win, FONT_ID, blind_message, 5, 5, 0, 0, ColourNameToRGB "darkred")
  elseif t.dark then
    WindowText (win, FONT_ID, dark_message, 5, 5, 0, 0, ColourNameToRGB "darkgreen")
  else  
    vertical = 5  -- pixel to start at
    if t.inroom then
  
      WindowRectOp (win, 2, 0, vertical, 0, vertical + #t.inroom.players * font_height, ColourNameToRGB "darkslategray")  
    
      table.sort (t.inroom.players, function (a, b) return a.dsc < b.dsc end)
      
      -- players first            
      for i, item in ipairs (t.inroom.players) do
        local colour = "white"
        WindowText (win, FONT_ID, item.dsc, 5, vertical, 0, 0, ColourNameToRGB (colour))
        vertical = vertical + font_height
      end -- for

      WindowRectOp (win, 2, 0, vertical, 0, vertical + #t.inroom.npcs * font_height, ColourNameToRGB "gainsboro") 

      table.sort (t.inroom.npcs, function (a, b) return a.dsc < b.dsc end)
      
      -- then NPCs
      for i, item in ipairs (t.inroom.npcs) do
        item.dsc = string.gsub (item.dsc, "[\r\n]", "")
        local colour = "crimson"
        WindowText (win, FONT_ID, item.dsc, 5, vertical, 0, 0, ColourNameToRGB (colour))
        vertical = vertical + font_height
      end -- for

      table.sort (t.inroom.objects, function (a, b) return a.dsc < b.dsc end)
                
      -- then objects in the room
      for i, item in ipairs (t.inroom.objects) do
        if item.typ then
          colour = item_colours [item.typ]
        end -- if item has a type
        WindowText (win, FONT_ID, item.dsc, 5, vertical, 0, 0, ColourNameToRGB (colour))
        vertical = vertical + font_height
      end -- for
        
    end -- if t.inroom table 
    
    if count == 0 then
      WindowText (win, FONT_ID, nothing_message, 5, 5, 0, 0, ColourNameToRGB "darkgreen")
    end -- if  
    
  end -- if 
  
  -- 3D box
  WindowCircleOp (win, 3, 0, 0, 0, 0, 0x505050, 0, 3, 0, 1)   -- dark grey border (3 pixels)
  WindowCircleOp (win, 3, 1, 1, -1, -1, 0x7C7C7C, 0, 1, 0, 1)  -- lighter inner border
  WindowCircleOp (win, 3, 2, 2, -2, -2, 0, 0, 1, 0, 1)  -- black inside that
  WindowLine (win, 1, 1, window_width - 1, 1, 0xC2C2C2, 0, 1)  -- light top edge
  WindowLine (win, 1, 1, 1, window_height - 1, 0xC2C2C2, 0, 1)  -- light left edge (for 3D look)
  
  -- make sure window visible
  WindowShow (win, true)

end -- function OnPluginTelnetOption

]]>
</script>

</muclient>
